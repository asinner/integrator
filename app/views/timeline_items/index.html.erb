<div class='timeline_items loop'>
	
	<div class='header'>
		<h2 class='heading'><%= @timeline.name %></h2>
		<%= link_to 'Add timeline event »', new_timeline_category_timeline_item_path(@timeline_category), class: 'new-link modal-link' %>
		
	</div>
	
	<div class='body'>
		<% if @timeline_items.reload.exists? %>

			<% @timeline_items.each do |timeline_item| %>

				<%= render 'timeline_items/single', timeline_item: timeline_item %>

			<% end %>

		<% else %>

			<span class='no-results'>No timeline items yet</span>

		<% end %>
	</div>
	
</div>