var form = $("#edit_timeline_<%= @timeline.id %>");

<% if @timeline.errors.any? %>

	<% @timeline.errors.each do |input, msg| %>
		// Find the field div containing the input
		var field = form.find("<%= ".field.#{input}" %>");
		// Give the field an error class
		field.addClass('error');
		// Put the error message into the placeholder
		field.find('.error-placeholder').html("<%= msg %>");
	<% end %>
	
<% else %>

	modalBackground();
	replaceRecord("<%= j render partial: 'timelines/single' %>", $("#timeline_<%= @timeline.id %>"));
	
<% end %>